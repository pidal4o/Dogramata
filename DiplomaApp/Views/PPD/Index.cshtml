@model IEnumerable<DiplomaApp.Models.PPD>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PPDType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PPDWidth)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PPDPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PPDType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PPDWidth)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PPDPrice)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PPDId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PPDId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PPDId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
